# AppAuto SaaS — Arquivo de Configuração de Ambiente
# =====================================================
# INSTRUÇÃO: Copie este arquivo como ".env" no servidor
# e preencha com os dados reais da sua hospedagem.
# NUNCA versione o arquivo .env com dados reais!
# =====================================================

APP_NAME=AppAuto
APP_ENV=prod
APP_DEBUG=false
APP_URL=https://erp.appauto.com.br

# Banco de Dados MySQL (Hostgator)
# Crie o banco no cPanel > MySQL Databases
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=NOME_DO_BANCO
DB_USERNAME=USUARIO_MYSQL
DB_PASSWORD=SENHA_MYSQL

# E-mail (usa mail() nativo do PHP por padrão)
MAIL_FROM=noreply@appauto.com.br
MAIL_FROM_NAME=AppAuto

# Consulta de placa — fontes opcionais (não versione chaves reais)
# O AppAuto tenta automaticamente cada fonte cuja configuração estiver completa.
# API Placas / WDAPI2: https://apiplacas.com.br/doc.php
APIPLACAS_TOKEN=

# APIBrasil: preencha endpoint e modalidade segundo a sua contratação/documentação.
APIBRASIL_TOKEN=
APIBRASIL_DEVICE_TOKEN=
APIBRASIL_VEHICLE_ENDPOINT=
APIBRASIL_VEHICLE_TYPE=

# PlacaAPI: use o endpoint contratado, mantendo os placeholders abaixo.
# Exemplo de placeholders aceitos: {placa}, {usuario}, {senha}
PLACAAPI_ENDPOINT=
PLACAAPI_USERNAME=
PLACAAPI_PASSWORD=

# Enriquecimento FIPE — desligado por padrão para evitar latência extra em consulta de placa
FIPE_AUTO_ENRICH=false
# FIPE v2 usa este host por padrão; configure token se a conta possuir um.
FIPE_API_BASE_URL=https://fipe.parallelum.com.br/api/v2
FIPE_API_TOKEN=

# OCR externo — ordem de fallback para documentos
OCR_PROVIDER_ORDER=cloudmersive,ocrspace,pixlab
ML_KIT_ENABLED=true

# Cloudmersive — leitura de placas e OCR de imagem
CLOUDMERSIVE_API_KEY=
CLOUDMERSIVE_BASE_URL=https://api.cloudmersive.com

# OCR.Space — OCR geral de imagem/PDF
OCRSPACE_API_KEY=
OCRSPACE_ENDPOINT=https://api.ocr.space/parse/image
OCRSPACE_URL_ENDPOINT=https://api.ocr.space/parse/imageurl
OCRSPACE_LANGUAGE=por

# PixLab — OCR por URL HTTPS
PIXLAB_API_KEY=
PIXLAB_OCR_ENDPOINT=https://api.pixlab.io/ocr
PIXLAB_LANGUAGE=pt
